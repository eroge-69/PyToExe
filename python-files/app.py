# Set your input and output file  file (list of IPs:Ports)
output_f    # Your input file (list of IPs:Ports)
output_filename = 'output.txt'  # Output file for filtered results

with open(ie, 'w') as outfirip()
        # Win infile:
        line = line.strip()
        # Win infile:
        line = line.strip()
        # Write the line if it doesn't end with port 3389
        if not line.endswith(':3389'):
            outfile.write(line + '\n')

print('All IPs with port 3389 have been removed. The filtered list is saved in output.txt.')
