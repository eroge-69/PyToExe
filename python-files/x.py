#!/usr/bin/env python3
"""
Converted from batch script
"""

import subprocess
import sys

def main():
    """Main function to execute all commands"""

    subprocess.run(["sc", "stop", "AJRouter"], text=True)
    subprocess.run(["sc", "config", "AJRouter", "start", "disabled"], text=True)
    subprocess.run(["sc", "stop", "AppMgmt"], text=True)
    subprocess.run(["sc", "config", "AppMgmt", "start", "disabled"], text=True)
    subprocess.run(["sc", "stop", "tzautoupdate"], text=True)
    subprocess.run(["sc", "config", "tzautoupdate", "start", "disabled"], text=True)
    subprocess.run(["sc", "stop", "BITS"], text=True)
    subprocess.run(["sc", "config", "BITS", "start", "disabled"], text=True)
    subprocess.run(["sc", "stop", "PeerDistSvc"], text=True)
    subprocess.run(["sc", "config", "PeerDistSvc", "start", "disabled"], text=True)
    subprocess.run(["sc", "stop", "DiagTrack"], text=True)
    subprocess.run(["sc", "config", "DiagTrack", "start", "disabled"], text=True)
    subprocess.run(["sc", "stop", "CDPSvc"], text=True)
    subprocess.run(["sc", "config", "CDPSvc", "start", "disabled"], text=True)
    subprocess.run(["sc", "stop", "DusmSvc"], text=True)
    subprocess.run(["sc", "config", "DusmSvc", "start", "disabled"], text=True)
    subprocess.run(["sc", "stop", "DoSvc"], text=True)
    subprocess.run(["sc", "config", "DoSvc", "start", "disabled"], text=True)
    subprocess.run(["sc", "stop", "diagsvc"], text=True)
    subprocess.run(["sc", "config", "diagsvc", "start", "disabled"], text=True)
    subprocess.run(["sc", "stop", "VaultSvc"], text=True)
    subprocess.run(["sc", "config", "VaultSvc", "start", "disabled"], text=True)
    subprocess.run(["sc", "stop", "DPS"], text=True)
    subprocess.run(["sc", "config", "DPS", "start", "disabled"], text=True)
    subprocess.run(["sc", "stop", "WdiServiceHost"], text=True)
    subprocess.run(["sc", "config", "WdiServiceHost", "start", "disabled"], text=True)
    subprocess.run(["sc", "stop", "WdiSystemHost"], text=True)
    subprocess.run(["sc", "config", "WdiSystemHost", "start", "disabled"], text=True)
    subprocess.run(["sc", "stop", "TrkWks"], text=True)
    subprocess.run(["sc", "config", "TrkWks", "start", "disabled"], text=True)
    subprocess.run(["sc", "stop", "MSDTC"], text=True)
    subprocess.run(["sc", "config", "MSDTC", "start", "disabled"], text=True)
    subprocess.run(["sc", "stop", "dmwappushservice"], text=True)
    subprocess.run(["sc", "config", "dmwappushservice", "start", "disabled"], text=True)
    subprocess.run(["sc", "stop", "DisplayEnhancementService"], text=True)
    subprocess.run(["sc", "config", "DisplayEnhancementService", "start", "disabled"], text=True)
    subprocess.run(["sc", "stop", "MapsBroker"], text=True)
    subprocess.run(["sc", "config", "MapsBroker", "start", "disabled"], text=True)
    subprocess.run(["sc", "stop", "fhsvc"], text=True)
    subprocess.run(["sc", "config", "fhsvc", "start", "disabled"], text=True)
    subprocess.run(["sc", "stop", "fdPHost"], text=True)
    subprocess.run(["sc", "config", "fdPHost", "start", "disabled"], text=True)
    subprocess.run(["sc", "stop", "EFS"], text=True)
    subprocess.run(["sc", "config", "EFS", "start", "disabled"], text=True)
    subprocess.run(["sc", "stop", "lfsvc"], text=True)
    subprocess.run(["sc", "config", "lfsvc", "start", "disabled"], text=True)
    subprocess.run(["sc", "stop", "HomeGroupListener"], text=True)
    subprocess.run(["sc", "config", "HomeGroupListener", "start", "disabled"], text=True)
    subprocess.run(["sc", "stop", "HomeGroupProvider"], text=True)
    subprocess.run(["sc", "config", "HomeGroupProvider", "start", "disabled"], text=True)
    subprocess.run(["sc", "stop", "vmickvpexchange"], text=True)
    subprocess.run(["sc", "config", "vmickvpexchange", "start", "disabled"], text=True)
    subprocess.run(["sc", "stop", "vmicguestinterface"], text=True)
    subprocess.run(["sc", "config", "vmicguestinterface", "start", "disabled"], text=True)
    subprocess.run(["sc", "stop", "vmicshutdown"], text=True)
    subprocess.run(["sc", "config", "vmicshutdown", "start", "disabled"], text=True)
    subprocess.run(["sc", "stop", "vmicheartbeat"], text=True)
    subprocess.run(["sc", "config", "vmicheartbeat", "start", "disabled"], text=True)
    subprocess.run(["sc", "stop", "vmicvmsession"], text=True)
    subprocess.run(["sc", "config", "vmicvmsession", "start", "disabled"], text=True)
    subprocess.run(["sc", "stop", "vmicrdv"], text=True)
    subprocess.run(["sc", "config", "vmicrdv", "start", "disabled"], text=True)
    subprocess.run(["sc", "stop", "vmictimesync"], text=True)
    subprocess.run(["sc", "config", "vmictimesync", "start", "disabled"], text=True)
    subprocess.run(["sc", "stop", "vmicvss"], text=True)
    subprocess.run(["sc", "config", "vmicvss", "start", "disabled"], text=True)
    subprocess.run(["sc", "stop", "IEEtwCollectorService"], text=True)
    subprocess.run(["sc", "config", "IEEtwCollectorService", "start", "disabled"], text=True)
    subprocess.run(["sc", "stop", "iphlpsvc"], text=True)
    subprocess.run(["sc", "config", "iphlpsvc", "start", "disabled"], text=True)
    subprocess.run(["sc", "stop", "PolicyAgent"], text=True)
    subprocess.run(["sc", "config", "PolicyAgent", "start", "disabled"], text=True)
    subprocess.run(["sc", "stop", "SharedAccess"], text=True)
    subprocess.run(["sc", "config", "SharedAccess", "start", "disabled"], text=True)
    subprocess.run(["sc", "stop", "lltdsvc"], text=True)
    subprocess.run(["sc", "config", "lltdsvc", "start", "disabled"], text=True)
    subprocess.run(["sc", "stop", "SmsRouter"], text=True)
    subprocess.run(["sc", "config", "SmsRouter", "start", "disabled"], text=True)
    subprocess.run(["sc", "stop", "CscService"], text=True)
    subprocess.run(["sc", "config", "CscService", "start", "disabled"], text=True)
    subprocess.run(["sc", "stop", "SEMgrSvc"], text=True)
    subprocess.run(["sc", "config", "SEMgrSvc", "start", "disabled"], text=True)
    subprocess.run(["sc", "stop", "PNRPsvc"], text=True)
    subprocess.run(["sc", "config", "PNRPsvc", "start", "disabled"], text=True)
    subprocess.run(["sc", "stop", "p2psvc"], text=True)
    subprocess.run(["sc", "config", "p2psvc", "start", "disabled"], text=True)
    subprocess.run(["sc", "stop", "p2pimsvc"], text=True)
    subprocess.run(["sc", "config", "p2pimsvc", "start", "disabled"], text=True)
    subprocess.run(["sc", "stop", "pla"], text=True)
    subprocess.run(["sc", "config", "pla", "start", "disabled"], text=True)
    subprocess.run(["sc", "stop", "PhoneSvc"], text=True)
    subprocess.run(["sc", "config", "PhoneSvc", "start", "disabled"], text=True)
    subprocess.run(["sc", "stop", "WpcMonSvc"], text=True)
    subprocess.run(["sc", "config", "WpcMonSvc", "start", "disabled"], text=True)
    subprocess.run(["sc", "stop", "WPDBusEnum"], text=True)
    subprocess.run(["sc", "config", "WPDBusEnum", "start", "disabled"], text=True)
    subprocess.run(["sc", "stop", "QWAVE"], text=True)
    subprocess.run(["sc", "config", "QWAVE", "start", "disabled"], text=True)
    subprocess.run(["sc", "stop", "RasAuto"], text=True)
    subprocess.run(["sc", "config", "RasAuto", "start", "disabled"], text=True)
    subprocess.run(["sc", "stop", "RasMan"], text=True)
    subprocess.run(["sc", "config", "RasMan", "start", "disabled"], text=True)
    subprocess.run(["sc", "stop", "SessionEnv"], text=True)
    subprocess.run(["sc", "config", "SessionEnv", "start", "disabled"], text=True)
    subprocess.run(["sc", "stop", "TermService"], text=True)
    subprocess.run(["sc", "config", "TermService", "start", "disabled"], text=True)
    subprocess.run(["sc", "stop", "RpcLocator"], text=True)
    subprocess.run(["sc", "config", "RpcLocator", "start", "disabled"], text=True)
    subprocess.run(["sc", "stop", "RemoteRegistry"], text=True)
    subprocess.run(["sc", "config", "RemoteRegistry", "start", "disabled"], text=True)
    subprocess.run(["sc", "stop", "RetailDemo"], text=True)
    subprocess.run(["sc", "config", "RetailDemo", "start", "disabled"], text=True)
    subprocess.run(["sc", "stop", "SysMain"], text=True)
    subprocess.run(["sc", "config", "SysMain", "start", "disabled"], text=True)
    subprocess.run(["sc", "stop", "WalletService"], text=True)
    subprocess.run(["sc", "config", "WalletService", "start", "disabled"], text=True)
    subprocess.run(["sc", "stop", "RemoteAccess"], text=True)
    subprocess.run(["sc", "config", "RemoteAccess", "start", "disabled"], text=True)
    subprocess.run(["sc", "stop", "WerSvc"], text=True)
    subprocess.run(["sc", "config", "WerSvc", "start", "disabled"], text=True)
    subprocess.run(["sc", "stop", "LanmanServer"], text=True)
    subprocess.run(["sc", "config", "LanmanServer", "start", "disabled"], text=True)
    subprocess.run(["sc", "stop", "WSearch"], text=True)
    subprocess.run(["sc", "config", "WSearch", "start", "disabled"], text=True)
    subprocess.run(["sc", "stop", "W32Time"], text=True)
    subprocess.run(["sc", "config", "W32Time", "start", "disabled"], text=True)
    subprocess.run(["sc", "stop", "icssvc"], text=True)
    subprocess.run(["sc", "config", "icssvc", "start", "disabled"], text=True)
    subprocess.run(["sc", "stop", "bthserv"], text=True)
    subprocess.run(["sc", "config", "bthserv", "start", "disabled"], text=True)
    subprocess.run(["sc", "stop", "StorSvc"], text=True)
    subprocess.run(["sc", "config", "StorSvc", "start", "disabled"], text=True)
    subprocess.run(["sc", "stop", "Spooler"], text=True)
    subprocess.run(["sc", "config", "Spooler", "start", "disabled"], text=True)
    subprocess.run(["sc", "stop", "Fax"], text=True)
    subprocess.run(["sc", "config", "Fax", "start", "disabled"], text=True)
    subprocess.run(["sc", "stop", "wisvc"], text=True)
    subprocess.run(["sc", "config", "wisvc", "start", "disabled"], text=True)
    subprocess.run(["sc", "stop", "lmhosts"], text=True)
    subprocess.run(["sc", "config", "lmhosts", "start", "disabled"], text=True)
    subprocess.run(["sc", "stop", "TapiSrv"], text=True)
    subprocess.run(["sc", "config", "TapiSrv", "start", "disabled"], text=True)
    subprocess.run(["sc", "stop", "Themes"], text=True)
    subprocess.run(["sc", "config", "Themes", "start", "disabled"], text=True)
    subprocess.run(["sc", "stop", "TabletInputService"], text=True)
    subprocess.run(["sc", "config", "TabletInputService", "start", "disabled"], text=True)
    subprocess.run(["sc", "stop", "WebClient"], text=True)
    subprocess.run(["sc", "config", "WebClient", "start", "disabled"], text=True)
    subprocess.run(["sc", "stop", "wcncsvc"], text=True)
    subprocess.run(["sc", "config", "wcncsvc", "start", "disabled"], text=True)
    subprocess.run(["sc", "stop", "ALG"], text=True)
    subprocess.run(["sc", "config", "ALG", "start", "disabled"], text=True)
    subprocess.run(["sc", "stop", "SCardSvr"], text=True)
    subprocess.run(["sc", "config", "SCardSvr", "start", "disabled"], text=True)
    subprocess.run(["sc", "stop", "StiSvc"], text=True)
    subprocess.run(["sc", "config", "StiSvc", "start", "disabled"], text=True)
    subprocess.run(["sc", "stop", "wscsvc"], text=True)
    subprocess.run(["sc", "config", "wscsvc", "start", "disabled"], text=True)
    subprocess.run(["sc", "stop", "WMPNetworkSvc"], text=True)
    subprocess.run(["sc", "config", "WMPNetworkSvc", "start", "disabled"], text=True)
    subprocess.run(["sc", "stop", "WbioSrvc"], text=True)
    subprocess.run(["sc", "config", "WbioSrvc", "start", "disabled"], text=True)
    subprocess.run(["sc", "stop", "FontCache3.0.0.0"], text=True)
    subprocess.run(["sc", "config", "FontCache3.0.0.0", "start", "disabled"], text=True)
    subprocess.run(["sc", "stop", "PcaSvc"], text=True)
    subprocess.run(["sc", "config", "PcaSvc", "start", "disabled"], text=True)
    subprocess.run(["sc", "stop", "EntAppSvc"], text=True)
    subprocess.run(["sc", "config", "EntAppSvc", "start", "disabled"], text=True)
    subprocess.run(["sc", "stop", "CertPropSvc"], text=True)
    subprocess.run(["sc", "config", "CertPropSvc", "start", "disabled"], text=True)
    subprocess.run(["sc", "stop", "ScDeviceEnum"], text=True)
    subprocess.run(["sc", "config", "ScDeviceEnum", "start", "disabled"], text=True)
    subprocess.run(["sc", "stop", "WinRM"], text=True)
    subprocess.run(["sc", "config", "WinRM", "start", "disabled"], text=True)
    subprocess.run(["sc", "stop", "wuauserv"], text=True)
    subprocess.run(["sc", "config", "wuauserv", "start", "disabled"], text=True)
    subprocess.run(["sc", "stop", "SCPolicySvc"], text=True)
    subprocess.run(["sc", "config", "SCPolicySvc", "start", "disabled"], text=True)
    subprocess.run(["sc", "stop", "BDESVC"], text=True)
    subprocess.run(["sc", "config", "BDESVC", "start", "disabled"], text=True)
    subprocess.run(["sc", "stop", "wbengine"], text=True)
    subprocess.run(["sc", "config", "wbengine", "start", "disabled"], text=True)
    subprocess.run(["sc", "stop", "XblAuthManager"], text=True)
    subprocess.run(["sc", "config", "XblAuthManager", "start", "disabled"], text=True)
    subprocess.run(["sc", "stop", "BTAGService"], text=True)
    subprocess.run(["sc", "config", "BTAGService", "start", "disabled"], text=True)
    subprocess.run(["sc", "stop", "FDResPub"], text=True)
    subprocess.run(["sc", "config", "FDResPub", "start", "disabled"], text=True)
    subprocess.run(["sc", "stop", "diagnosticshub.standardcollector.service"], text=True)
    subprocess.run(["sc", "config", "diagnosticshub.standardcollector.service", "start", "disabled"], text=True)
    subprocess.run(["sc", "stop", "MSiSCSI"], text=True)
    subprocess.run(["sc", "config", "MSiSCSI", "start", "disabled"], text=True)
    subprocess.run(["sc", "stop", "smphost"], text=True)
    subprocess.run(["sc", "config", "smphost", "start", "disabled"], text=True)
    subprocess.run(["sc", "stop", "NaturalAuthentication"], text=True)
    subprocess.run(["sc", "config", "NaturalAuthentication", "start", "disabled"], text=True)
    subprocess.run(["sc", "stop", "PrintNotify"], text=True)
    subprocess.run(["sc", "config", "PrintNotify", "start", "disabled"], text=True)
    subprocess.run(["sc", "stop", "XblGameSave"], text=True)
    subprocess.run(["sc", "config", "XblGameSave", "start", "disabled"], text=True)
    subprocess.run(["sc", "stop", "XboxNetApiSvc"], text=True)
    subprocess.run(["sc", "config", "XboxNetApiSvc", "start", "disabled"], text=True)
    subprocess.run(["sc", "stop", "shpamsvc"], text=True)
    subprocess.run(["sc", "config", "shpamsvc", "start", "disabled"], text=True)
    subprocess.run(["sc", "stop", "ShellHWDetection"], text=True)
    subprocess.run(["sc", "config", "ShellHWDetection", "start", "disabled"], text=True)
    subprocess.run(["sc", "stop", "SNMPTRAP"], text=True)
    subprocess.run(["sc", "config", "SNMPTRAP", "start", "disabled"], text=True)
    subprocess.run(["sc", "stop", "SDRSVC"], text=True)
    subprocess.run(["sc", "config", "SDRSVC", "start", "disabled"], text=True)
    subprocess.run(["sc", "stop", "TrkWks"], text=True)
    subprocess.run(["sc", "config", "TrkWks", "start", "disabled"], text=True)
    subprocess.run(["sc", "stop", "CryptSvc"], text=True)
    subprocess.run(["sc", "config", "CryptSvc", "start", "disabled"], text=True)
    print("\n[SUCCESS] BEAST Extreme Service Tweaks applied successfully!\n")

if __name__ == "__main__":
    try:
        main()
    except Exception as e:
        print(f"Error: {e}")
        sys.exit(1)