import random
from collections import defaultdict

def pairwise_selection(items):
    if not items:
        return None

    original_items = items.copy()
    elimination_order = []  # В каком порядке выбывали варианты
    current_round = 1
    stats = defaultdict(int)  # Для статистики (не используется в текущей версии)

    print(f"""Привет! Не можешь определиться че тебе сожрать? Давай я помогу тебе. 
    Я буду предлагать тебе 2 варианта еды, а ты вводи 1 или 2 чтобы выбрать - что из перечисленного тебе хотелось бы больше. 
          
    Играй пока игра не закончится, либо пока ты не определишься, что ты хочешь.""")

    while len(items) > 1:
        print(f"\n--- Раунд {current_round} ---")
        print(f"Осталось вариантов: {len(items)}")
        
        pair = random.sample(items, 2)
        print(f"Что вы предпочитаете?")
        print(f"1: {pair[0]}")
        print(f"2: {pair[1]}")
        
        while True:
            choice = input("Введите номер варианта (1 или 2): ").strip()
            if choice in ('1', '2'):
                break
            print("Ошибка: введите 1 или 2")
        
        # Определяем проигравший и сохраняем его
        loser_index = 1 if choice == '1' else 0
        loser = pair[loser_index]
        elimination_order.append(loser)
        items.remove(loser)
        
        print(f"Удалён вариант: {loser}")
        current_round += 1

    # Определяем топ-5 (последние выбывшие + победитель)
    winner = items[0]
    all_ranked = elimination_order + [winner]  # Первый выбыл раньше всех, победитель - последний
    top_5 = all_ranked[-5:]  # Берем последние 5 элементов (может быть меньше 5)
    top_5.reverse()  # Чтобы победитель был на первом месте

    # Форматируем вывод
    print("\n" + "="*50)
    print(f"Исходные варианты: {', '.join(original_items)}")
    print("\n🏆 ТОП-5 результатов:")
    for i, item in enumerate(top_5, 1):
        place = "1st" if i == 1 else "2nd" if i == 2 else "3rd" if i == 3 else f"{i}th"
        print(f"{place}: {item}{' (ПОБЕДИТЕЛЬ)' if i == 1 else ''}")
    
    print("\nПолный порядок выбывания:")
    print(" → ".join(elimination_order) + f" → {winner}")
    print("="*50)
    
    return winner, top_5

if __name__ == "__main__":
    food_options = ["Гречка с мясом и грибами", "Пицца", "Роллы", "Горячие роллы", "Шаурма", "Мачка", "KFC", "Бургер Кинг", "Сабвей", "Хачапури", "Хинкали", "Оливье", "Окрошка", "Обожраться рандомных снеков из лавки", "Шоколадный торт", "Том Ям", "Фо Бо", "Фиш энд Чипс", "Суп Гаспачо", "Борщ", "Солянка", "Харчо", "Селёдка с картошкой", "Теремок или Мистер Блин", "Блины домашние со всяким", "Сырники", "Оладьи", "Варёные яйца", "Тако\Буррито", "Шакшука", "Глазунья или Омлет", "Сэндвич домашний", "Сэндвич из магаза", "Паста Болоньезе или Карбонара", "Домашние макарохи со всякой ерундой", "Греческая кухня", "Варёная картоха и мясо", "Домашний бургер", "Поке", "Пончики\Пышки", "Чизкейк", "Жареные колбаски", "Шашлык", "Погрызть свежие овощи", "Жареная картошка", "Пельмени", "Вареники", "Мак-н-чиз", "Гёдза", "Рис с яйцом и всяким хрючевом", "Ризотто", "Картофельное пюре"]
    winner, top_5 = pairwise_selection(food_options)