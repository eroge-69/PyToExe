
import tkinter as tk

root = tk.Tk()
root.title("Battlefield 6 - BETA 0.17.0")
root.geometry("500x300")
label = tk.Label(root, text="Connecting to EA servers...", font=("Arial", 16))
label.pack(expand=True)

root.mainloop()
