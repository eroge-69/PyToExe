import scratchattach as sa
import os
import art
from art import *
import time

os.system('cls')
tprint("Sinewone")
print("The #5491 Scratch Botting Tool 😭")
print("Created by: xelna")
print("")

print('10, 20')
print('')
print('Total Bots Available:')
print('20')
print('')

user = input("User to bot: ")
amount = input("Amount of bots: ")

if amount == "10":
    os.system('cls')
    print("Verifying...")

    p = "testingbot123"
    session = sa.login('JXrocc73', p)
    user = session.connect_user(user)
    user.follow()
    os.system('cls')
    print("Followed user with 1st bot.")

    print("Logging in to next bot...")
    session = sa.login('oykbFEYv', p)
    user = session.connect_user(user)
    user.follow()
    print("Followed user with 2nd bot.")

    print("Logging in to next bot...")
    session = sa.login('JO3OYdNf', p)
    user = session.connect_user(user)
    user.follow()
    print("Followed user with 3rd bot.")

    print("Logging in to next bot...")
    session = sa.login('PrxVNB3v', p)
    user = session.connect_user(user)
    user.follow()
    print("Followed user with 4th bot.")

    print("Logging in to next bot...")
    session = sa.login('T8VjDIMa', p)
    user = session.connect_user(user)
    user.follow()
    print("Followed user with 5th bot.")

    print("Logging in to next bot...")
    session = sa.login('VTajivEN', p)
    user = session.connect_user(user)
    user.follow()
    print("Followed user with 6th bot.")

    print("Logging in to next bot...")
    session = sa.login('PQKutH1q', p)
    user = session.connect_user(user)
    user.follow()
    print("Followed user with 7th bot.")

    print("Logging in to next bot...")
    session = sa.login('PACmyeci', p)
    user = session.connect_user(user)
    user.follow()
    print("Followed user with 8th bot.")

    print("Logging in to next bot...")
    session = sa.login('BMdWvrFs', p)
    user = session.connect_user(user)
    user.follow()
    print("Followed user with 9th bot.")

    print("Logging in to next bot...")
    session = sa.login('Xo9GHrsc', p)
    user = session.connect_user(user)
    user.follow()
    print("Followed user with 10th bot.")
    time.sleep(1)

    os.system('cls')
    tprint("FINISHED")
else:
    if amount == "20":
        os.system('cls')
        print("Verifying...")

        p = "testingbot123"
        session = sa.login('JXrocc73', p)
        user = session.connect_user(user)
        user.follow()
        os.system('cls')
        print("Followed user with 1st bot.")

        print("Logging in to next bot...")
        session = sa.login('oykbFEYv', p)
        user = session.connect_user(user)
        user.follow()
        print("Followed user with 2nd bot.")

        print("Logging in to next bot...")
        session = sa.login('JO3OYdNf', p)
        user = session.connect_user(user)
        user.follow()
        print("Followed user with 3rd bot.")

        print("Logging in to next bot...")
        session = sa.login('PrxVNB3v', p)
        user = session.connect_user(user)
        user.follow()
        print("Followed user with 4th bot.")

        print("Logging in to next bot...")
        session = sa.login('T8VjDIMa', p)
        user = session.connect_user(user)
        user.follow()
        print("Followed user with 5th bot.")

        print("Logging in to next bot...")
        session = sa.login('VTajivEN', p)
        user = session.connect_user(user)
        user.follow()
        print("Followed user with 6th bot.")

        print("Logging in to next bot...")
        session = sa.login('PQKutH1q', p)
        user = session.connect_user(user)
        user.follow()
        print("Followed user with 7th bot.")

        print("Logging in to next bot...")
        session = sa.login('PACmyeci', p)
        user = session.connect_user(user)
        user.follow()
        print("Followed user with 8th bot.")

        print("Logging in to next bot...")
        session = sa.login('BMdWvrFs', p)
        user = session.connect_user(user)
        user.follow()
        print("Followed user with 9th bot.")

        print("Logging in to next bot...")
        session = sa.login('Xo9GHrsc', p)
        user = session.connect_user(user)
        user.follow()
        print("Followed user with 10th bot.")

        print("Logging in to next bot...")
        session = sa.login('9Jczj8Ym', p)
        user = session.connect_user(user)
        user.follow()
        print("Followed user with 11th bot.")

        print("Logging in to next bot...")
        session = sa.login('LoBCjaar', p)
        user = session.connect_user(user)
        user.follow()
        print("Followed user with 12th bot.")

        print("Logging in to next bot...")
        session = sa.login('SuyOIa2V', p)
        user = session.connect_user(user)
        user.follow()
        print("Followed user with 13th bot.")

        print("Logging in to next bot...")
        session = sa.login('bGLvQJW7', p)
        user = session.connect_user(user)
        user.follow()
        print("Followed user with 14th bot.")

        print("Logging in to next bot...")
        session = sa.login('fFaDCpdv', p)
        user = session.connect_user(user)
        user.follow()
        print("Followed user with 15th bot.")

        print("Logging in to next bot...")
        session = sa.login('PNt2x5Qq', p)
        user = session.connect_user(user)
        user.follow()
        print("Followed user with 16th bot.")

        print("Logging in to next bot...")
        session = sa.login('YFbeXPUP', p)
        user = session.connect_user(user)
        user.follow()
        print("Followed user with 17th bot.")

        print("Logging in to next bot...")
        session = sa.login('vZlYM6xC', p)
        user = session.connect_user(user)
        user.follow()
        print("Followed user with 18th bot.")

        print("Logging in to next bot...")
        session = sa.login('Ob7ByzPL', p)
        user = session.connect_user(user)
        user.follow()
        print("Followed user with 19th bot.")

        print("Logging in to next bot...")
        session = sa.login('OodmlWsv', p)
        user = session.connect_user(user)
        user.follow()
        print("Followed user with 20th bot.")
        time.sleep(1)

        os.system('cls')
        tprint("FINISHED")
    else:
        os.system('cls')
        tprint("Oops!")
        print("Error, please try again.")